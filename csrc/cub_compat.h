/**
 * =============================================================================
 * CUB 라이브러리 호환성 헤더
 * =============================================================================
 *
 *
 * [CUB란 무엇인가?]
 *
 * CUB (CUDA UnBound)는 NVIDIA CUDA Toolkit에 포함된
 * 블록/워프 단위 병렬 프리미티브 라이브러리입니다.
 *
 * CUDA에서 자주 필요한 패턴들을 고성능으로 구현해 놓은 것입니다:
 *   - BlockReduce: 블록 내 모든 스레드의 값을 합산/최대/최소
 *   - BlockScan:   블록 내 prefix sum (누적합)
 *   - BlockSort:   블록 내 정렬
 *   - WarpReduce:  워프(32 스레드) 내 리덕션
 *   등등...
 *
 * 이것은 별도 설치가 필요 없으며, CUDA Toolkit을 설치하면
 * <cub/cub.cuh>로 바로 사용할 수 있습니다.
 *
 *
 * [왜 CUB가 필요한가? — 리덕션 문제]
 *
 * RMSNorm의 핵심 연산은 "한 행(hidden_size개 원소)의 제곱합"입니다:
 *   variance = x_0² + x_1² + x_2² + ... + x_{d-1}²
 *
 * 이것은 전형적인 **리덕션(reduction)** 문제입니다.
 * 여러 스레드가 각각 일부를 계산한 후, 모든 결과를 하나로 합쳐야 합니다.
 *
 *
 * ─────────────────────────────────────────────────────────────────
 * 배경 지식: GPU의 실행 모델과 메모리 구조
 * ─────────────────────────────────────────────────────────────────
 *
 * 이 내용을 이해하려면 GPU가 어떻게 스레드를 실행하고,
 * 스레드들이 어떻게 메모리를 공유하는지를 알아야 합니다.
 *
 *
 * [GPU 실행 단위: 스레드 → 워프 → 블록]
 *
 * GPU에서 코드를 실행하는 최소 단위는 **스레드(thread)**입니다.
 * 하지만 GPU 하드웨어는 스레드를 하나씩 개별로 실행하지 않습니다.
 *
 *   스레드(Thread): 하나의 실행 흐름. 각자 레지스터(변수)를 갖고 있음.
 *                   CPU 스레드와 비슷하지만, 훨씬 가볍고 수천~수만 개 동시 실행.
 *
 *   워프(Warp): 32개 스레드를 묶은 것. GPU가 실행하는 실제 최소 단위.
 *              같은 워프의 32개 스레드는 항상 동일한 명령어를 동시에 실행함.
 *              (SIMT: Single Instruction, Multiple Threads)
 *              CPU로 비유하면 SIMD(SSE/AVX)와 유사하지만, 스레드 단위라는 점이 다름.
 *
 *   블록(Block): 프로그래머가 정의하는 **논리적 그룹**. 여러 워프로 구성됨.
 *               블록 크기는 보통 128, 256, 512, 1024개 스레드.
 *
 *
 * [블록은 논리적 단위인가, 물리적 단위인가?]
 *
 * 결론부터 말하면: **블록은 논리적(소프트웨어) 단위**입니다.
 * 하지만 GPU 하드웨어가 블록을 물리적 SM에 매핑할 때,
 * 물리적 자원(공유 메모리, 레지스터)을 블록 단위로 할당합니다.
 *
 * 이 관계를 정확히 이해하려면 두 관점을 구분해야 합니다:
 *
 *   ┌───────────────────────────────────────────────────────────────┐
 *   │  프로그래머 관점 (논리적)                                           │
 *   │                                                               │
 *   │  커널을 launch할 때 블록 수와 크기를 지정합니다:                        │
 *   │    kernel<<<gridDim=100, blockDim=256>>>();                   │
 *   │    → "256개 스레드짜리 블록을 100개 만들어라"                         │
 *   │                                                               │
 *   │  프로그래머는:                                                   │
 *   │    - 어떤 블록이 어떤 SM에서 실행되는지 모름 (알 필요 없음)               │
 *   │    - 블록 간 실행 순서도 모름 (보장되지 않음)                          │
 *   │    - 블록 내에서만 스레드 간 통신(__shared__, __syncthreads)         │
 *   │                                                               │
 *   │  즉 블록은 "같이 협력할 수 있는 스레드의 논리적 묶음"입니다.               │
 *   └───────────────────────────────────────────────────────────────┘
 *
 *   ┌───────────────────────────────────────────────────────────────┐
 *   │  하드웨어 관점 (물리적 매핑)                                        │
 *   │                                                               │
 *   │  GPU에는 여러 개의 SM(Streaming Multiprocessor)이 있습니다.         │
 *   │  (예: A100은 108개, RTX 4090은 128개)                            │
 *   │                                                               │
 *   │  GPU 스케줄러가 하는 일:                                          │
 *   │    1. 블록을 빈 SM에 배정 (블록 → SM 매핑)                          │
 *   │    2. 그 SM의 물리적 SRAM에서 해당 블록 전용 영역을 할당                │
 *   │    3. 블록이 끝나면 자원을 회수하고, 다음 블록을 배정                    │
 *   │                                                               │
 *   │  하나의 SM에 블록이 여러 개 올라갈 수도 있습니다:                        │
 *   │    SM #0:  [블록3][블록17]  ← SRAM을 이 두 블록이 나눠 사용           │
 *   │    SM #1:  [블록8][블록42]                                      │
 *   │    SM #2:  [블록1]         ← 자원이 부족하면 1개만 배정              │
 *   │                                                               │
 *   │  핵심: 블록이 어디서 실행될지는 런타임에 GPU가 결정합니다.                 │
 *   │  프로그래머가 "블록5는 SM#2에서 실행해라"라고 지정할 수 없습니다.           │
 *   └───────────────────────────────────────────────────────────────┘
 *
 * CPU로 비유하면:
 *   - 블록 ≈ OS 스레드 (논리적 실행 단위)
 *   - SM  ≈ CPU 코어  (물리적 실행 장치)
 *   - OS 스레드를 어떤 코어에서 실행할지는 OS 스케줄러가 결정하듯,
 *     블록을 어떤 SM에서 실행할지는 GPU 스케줄러가 결정합니다.
 *
 * 예: 블록 크기가 256이면:
 *   블록 = [워프0(스레드0~31), 워프1(스레드32~63), ..., 워프7(스레드224~255)]
 *   → 8개 워프, 총 256개 스레드
 *
 *
 * [GPU 메모리 구조 — CPU의 shared memory와는 완전히 다름]
 *
 * ※ CPU의 shared memory는 프로세스 간 통신(IPC)을 위한 OS 기능입니다.
 *   GPU의 __shared__ 메모리는 이것과 전혀 다른 개념입니다!
 *
 * GPU 메모리 계층 (빠른 순서):
 *
 *   ┌─────────────┐  가장 빠름 (1 클록)
 *   │  레지스터     │  각 스레드 전용. 다른 스레드가 접근 불가.
 *   │  (Register) │  float variance = 0.0f; ← 이게 레지스터에 저장됨
 *   ├─────────────┤
 *   │  공유 메모리   │  같은 블록 내 스레드끼리만 공유. (~5 클록)
 *   │ (__shared__)│  물리적으로 SM 칩 위에 있는 작은 SRAM (~48~100KB).
 *   │             │  블록 밖의 스레드는 접근 불가.
 *   │             │  CPU의 L1 캐시 크기/속도와 비슷하지만,
 *   │             │  프로그래머가 직접 제어한다는 점이 다름.
 *   ├─────────────┤
 *   │  글로벌 메모리 │  모든 스레드가 접근 가능. 가장 느림 (~200~400 클록)
 *   │  (Global)   │  GPU의 DRAM (VRAM). torch.Tensor가 저장되는 곳.
 *   │             │  용량 크지만(8~80GB), 레이턴시가 매우 높음.
 *   └─────────────┘
 *
 * 핵심 차이:
 *   - CPU shared memory: OS가 관리하는 IPC 메커니즘 (mmap 기반, 프로세스 간)
 *   - GPU __shared__:    하드웨어 SRAM, 같은 블록 내 스레드 간 초고속 통신용
 *
 *
 * [__shared__ 메모리는 물리적인데, 어떻게 논리적 블록 단위로 동작하는가?]
 *
 * 이것이 핵심적인 의문입니다:
 *   "블록이 논리적 단위라면, __shared__도 논리적이어야 하지 않나?
 *    물리적 SRAM이라면 블록 단위로 나눠 쓰기 힘들지 않나?"
 *
 * 답: __shared__ 메모리는 **물리적 SRAM**이 맞지만,
 *     GPU 하드웨어가 **블록 스케줄링 시점에 자동으로 분할(partition)**합니다.
 *
 * 동작 과정:
 *
 *   1. 컴파일 시: nvcc가 커널의 __shared__ 변수들을 보고
 *      "이 커널은 블록당 공유 메모리 N 바이트가 필요하다"고 계산합니다.
 *
 *   2. 런타임 스케줄링 시: GPU 스케줄러가 블록을 SM에 배정할 때,
 *      SM의 SRAM에서 N 바이트를 해당 블록 전용으로 잘라서 할당합니다.
 *      다른 블록은 이 영역에 절대 접근할 수 없습니다 (하드웨어가 보장).
 *
 *   3. 블록 종료 시: 해당 SRAM 영역이 해제되고, 다음 블록에 재할당됩니다.
 *
 * 시각화:
 *
 *   SM #0의 SRAM (100KB 예시):
 *   ┌────────────────────────────────────────────────────────┐
 *   │ [블록 3 전용: 16KB]   │  [블록 17 전용: 16KB]     │ 여유     │
 *   │  warp_sums[8]       │  warp_sums[8]          │         │
 *   │  s_variance         │  s_variance            │         │
 *   └────────────────────────────────────────────────────────┘
 *        ↑ 블록3의 스레드만 접근      ↑ 블록17의 스레드만 접근
 *
 * 이것은 OS가 프로세스마다 가상 메모리를 할당하는 것과 비슷합니다:
 *   - 물리 RAM은 하나지만, 프로세스마다 독립된 메모리 공간이 보장됨
 *   - 물리 SRAM은 SM마다 하나지만, 블록마다 독립된 __shared__ 공간이 보장됨
 *
 * 그래서 코드에서 `__shared__ float s_variance;`라고 쓰면:
 *   - 블록 A의 s_variance와 블록 B의 s_variance는
 *     물리적으로 완전히 다른 SRAM 주소에 위치합니다.
 *   - 같은 소스 코드의 같은 변수명이지만, 블록마다 별도 인스턴스가 생기는 것입니다.
 *   - 이것은 함수의 지역 변수가 호출마다 별도 스택 프레임에 생기는 것과 같은 원리입니다.
 *
 *
 * [왜 "그냥 하나의 메모리에 더하면" 안 되는가?]
 *
 * 직관적으로 이렇게 하고 싶을 수 있습니다:
 *
 *   __shared__ float total = 0;           // 공유 변수 하나
 *   total += my_value;                    // 각 스레드가 더하기
 *
 * 이것이 안 되는 이유: **경쟁 상태(race condition)**.
 *
 * 256개 스레드가 동시에 total을 읽고, 더하고, 쓰면:
 *
 *   스레드 0: total 읽기(0) → 0 + 3.0 = 3.0 → total에 쓰기 ┐
 *   스레드 1: total 읽기(0) → 0 + 5.0 = 5.0 → total에 쓰기 ┤ 동시에!
 *   스레드 2: total 읽기(0) → 0 + 2.0 = 2.0 → total에 쓰기 ┘
 *
 *   기대값: 3.0 + 5.0 + 2.0 = 10.0
 *   실제값: 마지막에 쓴 스레드의 값만 남음 (예: 2.0) ← 데이터 손실!
 *
 * atomicAdd()를 쓸 수도 있지만:
 *   atomicAdd(&total, my_value);  // 원자적 덧셈 (경쟁 상태 방지)
 *
 * 이것은 모든 스레드가 하나의 주소에 순차적으로 접근하므로
 * 사실상 직렬 실행이 되어 병렬성을 완전히 잃습니다.
 * 256개 스레드가 줄 서서 하나씩 더하는 꼴 → CPU보다 느림.
 *
 *
 * [올바른 리덕션: 트리 구조로 병렬 합산]
 *
 * 해결책은 트리(tree) 구조로 단계적으로 합치는 것입니다:
 *
 *   8개 값을 합산하는 예 (스레드 0~7):
 *
 *   Step 1: 이웃끼리 합산 (4번의 병렬 덧셈)
 *     [a, b, c, d, e, f, g, h]
 *      ↘↙   ↘↙   ↘↙   ↘↙
 *     [a+b, c+d, e+f, g+h]
 *
 *   Step 2: 결과끼리 합산 (2번의 병렬 덧셈)
 *     [a+b, c+d, e+f, g+h]
 *       ↘↙       ↘↙
 *     [a+b+c+d, e+f+g+h]
 *
 *   Step 3: 최종 합산 (1번의 덧셈)
 *     [a+b+c+d, e+f+g+h]
 *          ↘↙
 *     [a+b+c+d+e+f+g+h]   ← 결과!
 *
 *   → 256개 값이면 8 단계(log2(256))만에 완료.
 *     순차적이면 255번 덧셈이 필요한데, 트리로는 8 단계.
 *
 * 그런데 이 트리 리덕션을 구현하려면:
 *
 *   1. 워프 내 리덕션: __shfl_xor_sync()
 *      같은 워프(32 스레드) 안에서는 레지스터 간 직접 값 교환이 가능합니다.
 *      공유 메모리를 거치지 않으므로 매우 빠릅니다 (1 클록).
 *      워프 안의 32개 값을 5 단계(log2(32))만에 합산.
 *
 *   2. 워프 간 리덕션: __shared__ 메모리
 *      다른 워프의 레지스터에는 직접 접근할 수 없습니다.
 *      그래서 각 워프의 결과를 __shared__ 메모리에 써놓고,
 *      대표 워프가 다시 읽어서 합산합니다.
 *
 *   3. __syncthreads(): 블록 내 전체 동기화
 *      "모든 스레드가 여기까지 실행을 마칠 때까지 대기"
 *      워프 간 리덕션 전에 호출해야 합니다.
 *      만약 동기화 없이 읽으면, 아직 쓰지 않은 워프의 값을
 *      쓰레기 값으로 읽게 됩니다.
 *
 * 전체 흐름 (256 스레드 = 8 워프):
 *
 *   Step 1: 워프 내 리덕션 (__shfl_xor_sync)
 *     워프0: 32개 값 → 1개 부분합
 *     워프1: 32개 값 → 1개 부분합
 *     ...
 *     워프7: 32개 값 → 1개 부분합
 *
 *   Step 2: 워프 간 리덕션 (__shared__ 메모리 경유)
 *     __shared__ float warp_sums[8];
 *     각 워프의 대표 스레드가 부분합을 warp_sums에 저장
 *     __syncthreads();  ← 모든 워프가 쓸 때까지 대기
 *     워프0이 warp_sums[0~7]을 읽어서 최종 합산
 *
 * CUB의 BlockReduce는 위 과정을 전부 내부적으로 처리합니다:
 *
 *   using BlockReduce = cub::BlockReduce<float, 1024>;
 *   __shared__ typename BlockReduce::TempStorage reduceStore;
 *   float total = BlockReduce(reduceStore).Reduce(my_value, cub::Sum());
 *
 * 한 줄로 블록 전체 리덕션이 완료됩니다.
 * TempStorage는 CUB가 내부적으로 사용하는 __shared__ 메모리 공간입니다.
 *
 *
 * [왜 RoPE에서는 CUB가 필요 없었는가?]
 *
 * RoPE와 RMSNorm의 연산 구조가 근본적으로 다릅니다:
 *
 * RoPE: 각 (x, y) 쌍을 독립적으로 회전
 *   → 스레드 간 데이터 교환이 전혀 없음
 *   → 각 스레드가 자기 담당 원소만 처리하면 끝
 *   → "embarrassingly parallel" (완전 병렬)
 *
 * RMSNorm: hidden_size개 원소의 제곱합을 구한 후, 그 값으로 나누기
 *   → 모든 스레드의 부분합을 하나로 모아야 함
 *   → 스레드 간 통신(리덕션)이 필수
 *   → CUB BlockReduce가 이 역할을 담당
 *
 * 정리하면:
 *   - RoPE = 원소별(element-wise) 독립 연산 → 리덕션 불필요
 *   - RMSNorm = 행 단위(row-wise) 집계 연산 → 리덕션 필수 → CUB 사용
 *
 *
 * [이 파일의 역할]
 *
 * CUB 2.0.8 이상에서 리덕션 연산자 API가 변경되었습니다:
 *   - 기존: cub::Sum, cub::Max 등의 펑터 클래스
 *   - 변경: cuda::std::plus<>, cuda::maximum<> 등 C++ 표준 함수 객체
 *
 * 이 헤더는 CUB 버전에 관계없이 동일한 코드를 사용할 수 있도록
 * CubAddOp 타입 별칭을 제공합니다.
 *
 * 참고: vLLM의 csrc/cub_helpers.h를 간소화한 버전입니다.
 */

#pragma once

#include <cub/cub.cuh>

#if CUB_VERSION >= 200800
  #include <cuda/std/functional>
  using CubAddOp = cuda::std::plus<>;
#else
  using CubAddOp = cub::Sum;
#endif
